SCREEN 01: High-fidelity desktop web UI for Desearch AI (1440x900 viewport), a calm, serious, editorial AI research workspace designed for evidence-backed synthesis. The layout is a persistent 3-column architecture featuring an expanded left navigation sidebar (260px width) and a main research workspace anchored by a dominant warm cream canvas background (#FBF9F5, hsl(38,30%,97%)). The sidebar is styled with clean white surface tone (#FFFFFF) and a subtle warm border (#E6E2D8) on the right; it contains a brand header with "Desearch AI" in clean medium sans-serif typography next to a small sidebar collapse icon control (Ctrl+[), a prominent "+ New Research" primary action button in terracotta coral (#D95338, rounded-md 6px radius), a sidebar search input ("Search research history..."), grouped historical sessions ("Today", "Yesterday", "Previous 7 Days", "Older") displaying compact text rows such as "Compare FastAPI vs Flask for production APIs" and "Vector database performance benchmarks" with relative timestamps, and footer shell affordances for Settings and User Account. The main research canvas is completely open and uncluttered, featuring a top-right header with a contextual "Reports" button displaying a muted source badge. The center of the warm cream canvas features a centered, high-quality research composer inspired by modern Raycast/Claude interaction quality: a clean off-white container (#FFFFFF, subtle shadow 0 2px 8px rgba(31,30,28,0.04), warm border #E6E2D8, rounded-lg 8px radius) with an auto-resizing text area displaying placeholder "Ask a research question or enter a topic...", a subtle mode badge "Deep Research", and a right circular terracotta coral submit button (#D95338, 32x32px, rounded-full) with a white upward arrow icon. Below the composer are three compact research-oriented example chips: "Compare FastAPI and Flask for production AI APIs", "Analyze vector database index performance", and "Evaluate serverless PostgreSQL cold-start latency". The typography uses Inter (humanist sans-serif) for all UI controls, labels, and inputs, with deep charcoal text (#1F1E1C) for high legibility. Strictly exclude generic SaaS dashboard widgets, deal cards, analytics charts, purple AI gradients, glassmorphism, floating blobs, Anthropic/Claude logos or wordmarks, model/provider dropdown selectors (openrouter/free), file attachment buttons, and Stop/Cancel buttons.

SCREEN 02: High-fidelity desktop web UI for Desearch AI (1440x900 viewport) capturing an active real-time research session during the strategy planning phase, preserving the exact 3-column shell, 260px expanded sidebar, warm cream canvas (#FBF9F5), and deep charcoal typography (#1F1E1C) established in SCREEN 01. In the main research canvas (constrained 720px line length reading area), the user prompt is displayed at top as a clear query heading: "Compare FastAPI and Flask for building production AI APIs." Directly below the query is the real-time research progress block: an open, clean container showing human-readable semantic execution stages. The first stage "Planning research strategy..." is the single active stage, featuring a subtle, luminous left-to-right text shimmer animation through the charcoal text. Future execution stages ("Searching sources...", "Reading sources...", "Writing report...", "Reviewing findings...") are listed below in muted warm grey text (#8C877E) with small muted dots. There is NO large numerical percentage progress bar, NO raw backend event strings (planner.started), NO developer console, and NO multiple glowing stages. The bottom-anchored composer is in a busy/disabled state with a soft muted background and helper text "Research in progress...", displaying a small rotating loading indicator in place of the submit arrow; there is NO Stop/Cancel button. The top-right header contains the contextual "Reports" button with a pulsing status badge. Strictly maintain visual continuity with SCREEN 01 including exact sidebar items, colors, fonts, and borders.

SCREEN 03: High-fidelity desktop web UI for Desearch AI (1440x900 viewport) capturing an active real-time research session during deep web evidence collection, continuing the exact visual language, 260px sidebar, and shell from SCREEN 01 and SCREEN 02. The user query "Compare FastAPI and Flask for building production AI APIs" remains at top. In the progress container, previous stages display completed static green checkmark icons (✓, deep olive green #2D6A4F): "Planning complete ✓" and "Searching sources complete ✓". The single current active research stage displays "Reading sources..." with a subtle luminous left-to-right text shimmer animation through the charcoal text. Below the active stage is a compact, clean row of live discovered research source badges showing domain names being analyzed ("fastapi.tiangolo.com", "flask.palletsprojects.com"). Future execution stages ("Writing report...", "Reviewing findings...") remain muted warm grey (#8C877E). Only the single current stage ("Reading sources...") shimmers. There are NO raw backend event names, NO HTTP status codes, NO raw API keys, NO dark developer console, and NO percentage bars. The bottom composer remains in a busy/disabled state ("Research in progress..."). Preserve strict open composition on the dominant warm cream canvas (#FBF9F5) with warm subtle borders (#E6E2D8) and deep charcoal typography (#1F1E1C).

SCREEN 04: High-fidelity desktop web UI for Desearch AI (1440x900 viewport) presenting a completed, synthesized research report, building directly on the execution sequence from SCREEN 01, 02, and 03. The top query remains "Compare FastAPI and Flask for building production AI APIs." The progress container has automatically collapsed into a quiet, compact summary bar: "✓ Research completed · 8 sources · 1m 42s" with a subtle [Expand Trace] link on the right. Below the summary bar is the synthesized research report presented as an open, readable editorial document (constrained 720px reading width, 1.6 line height, warm cream background #FBF9F5) rather than a speech chat bubble. The report features a prominent editorial title in EB Garamond serif typography (28px, semi-bold, #1F1E1C): "Production Readiness Comparison: FastAPI vs. Flask for AI Application Interfaces". The body text consists of well-structured paragraphs in Inter sans-serif, section headings in EB Garamond ("1. Async Concurrency & Throughput", "2. Data Validation & Developer Ergonomics", "3. Production Ecosystem & Deployment Risks"), a clean compact markdown comparison table with subtle warm borders (#E6E2D8), and numbered inline citations like [1] and [2] in compact terracotta coral badges (#D95338). Directly beneath the report text is a compact "Sources" section displaying small source cards with favicons, titles ("FastAPI Performance Benchmarks", "Flask Architecture Guide"), domain labels, and a "View all sources (8)" trigger. At the end of the report is a compact "Research Report Artifact" card (#FFFFFF surface, warm border #E6E2D8) with an "Open Report" button. At the bottom of the workspace, the composer is ready for new queries with placeholder text "Start another research question" (multi-turn chat is not implied). Maintain exact visual consistency with SCREEN 01–03.

SCREEN 05: High-fidelity desktop web UI for Desearch AI (1440x900 viewport) demonstrating evidence verification micro-UX with an active inline citation popover, built over the completed research workspace of SCREEN 04. The background workspace displays the user query, collapsed completion summary bar ("✓ Research completed · 8 sources · 1m 42s"), and editorial report text ("Production Readiness Comparison: FastAPI vs. Flask..."). Inline citation badge [1] in the body paragraph is in an active hover/focus state with a terracotta coral highlight (#D95338). Floating directly above/adjacent to citation [1] is the CitationPopover overlay card (#FFFFFF clean white surface, warm border #E6E2D8, subtle elevation shadow 0 4px 16px rgba(31,30,28,0.08), 320px width, 6px radius). The popover cleanly displays: source title "FastAPI Framework Performance Benchmarks", domain name "fastapi.tiangolo.com", a short 2-line textual excerpt snippet ("FastAPI utilizes Starlette and Pydantic to achieve throughput comparable to NodeJS and Go under high async I/O concurrency..."), and an external link action "[Open Source ↗]" in terracotta coral. The citation popover MUST NOT display any synthetic authority scores, confidence percentages, trust ratings, or fake verification badges. The rest of the workspace and 260px expanded sidebar remain contextually visible behind the popover. Preserve exact visual identity, colors (#FBF9F5 canvas, #1F1E1C charcoal text), fonts, and shell structure from SCREEN 01–04.

SCREEN 06: High-fidelity desktop web UI for Desearch AI (1440x900 viewport) presenting the contextual Reports and Evidence Drawer open, floating over the completed research workspace of SCREEN 04 and 05. The main workspace (user query, report text, citations) is visible on the left behind a soft subtle backdrop scrim. Sliding out from the right is the ReportsDrawer panel (420px width, clean off-white surface #FFFFFF, left warm border #E6E2D8, elevation shadow -4px 0 24px rgba(31,30,28,0.08)). The drawer header displays "Reports & Evidence" with a close "X" button. The drawer content includes: a Canonical Research Report summary card ("FastAPI vs Flask Synthesis Report", 8 sources, 520 words) with a prominent "Open Full Report" button (terracotta coral CTA #D95338), an "Export Formats" section with two explicit download action buttons "[⬇ Download PDF (.pdf)]" and "[⬇ Download Markdown (.md)]" (NO DOCX or HTML buttons), and a "Research Sources" section displaying a vertical list of all 8 consulted source items with favicons, titles ("FastAPI Docs", "Flask Benchmarks"), domain URLs, and short snippets. Terminology strictly uses "Research Sources" (NOT "Verified Sources"). The 260px left sidebar remains visible on the far left. Preserve 100% color, font, and layout alignment with SCREEN 01–05.

SCREEN 07: High-fidelity desktop web UI for Desearch AI (1440x900 viewport) presenting the Expanded Report Viewer modal overlay for deep long-form report reading, launched from SCREEN 06. The research workspace and sidebar are contextually visible in the background behind a semi-transparent warm backdrop overlay. Occupying approximately 60% of the viewport width in the center is the ReportViewer modal container (#FFFFFF surface, warm border #E6E2D8, shadow 0 8px 32px rgba(31,30,28,0.12), rounded-lg 8px radius). The viewer header displays the report metadata ("Canonical Synthesis Report · 8 Sources · Updated Today"), PDF and Markdown export action buttons, and a prominent top-right close "X" button. Below the header, the layout is split into a compact sticky Table of Contents sidebar navigation on the left ("1. Concurrency", "2. Validation", "3. Ecosystem", "4. Sources") and a spacious long-form editorial reading canvas on the right formatted in EB Garamond serif typography (32px title, 22px H2 headings, 16px body, 1.6 line height). The report content displays full markdown sections, comparison tables, inline citation numbers [1] [2], and a complete Bibliography/Sources Index at the bottom. Do NOT attempt to squeeze the report into a narrow drawer; use the expanded 60% modal reading canvas. Maintain exact brand colors (#FBF9F5, #1F1E1C, #D95338) and font pairings established in SCREEN 01–06.

SCREEN 08: High-fidelity desktop web UI for Desearch AI (1440x900 viewport) focusing on research session history management inside the expanded left sidebar, preserving the main research workspace on the right from SCREEN 04. The focus is on the 260px expanded left sidebar: the history search input ("Search research history...") is active with typed text "FastAPI". The chronological session list below filters dynamically, showing grouped headers "Today", "Yesterday", "Previous 7 Days", "Older". The active session row ("Compare FastAPI and Flask for production AI APIs") is highlighted with a subtle warm background fill (#F5F2EC) and a thin terracotta coral left indicator line (#D95338). A second session row ("Vector database index performance") is in a hovered state, revealing compact inline action icons for "Rename" (pencil icon) and "Delete" (trash icon). Clicking Delete shows a small popover confirmation tooltip ("Delete session? [Cancel] [Delete]"). The sidebar does NOT contain unbacked SaaS navigation items (Dashboard, Analytics, CRM, Deals) or unbacked session actions (Pin, Share, Duplicate, Archive). The main research workspace remains contextually visible on the right. Maintain strict visual continuity, typography (Inter), colors (#FBF9F5 canvas, #FFFFFF sidebar, #1F1E1C text), and spacing from SCREEN 01–07.

SCREEN 09: High-fidelity desktop web UI for Desearch AI (1440x900 viewport) demonstrating graceful, calm failure handling when a research workflow encounters an error, maintaining the exact shell, 260px sidebar, and typography from SCREEN 01–08. The user query is rendered at top: "Compare FastAPI and Flask for building production AI APIs." Below the query, the research progress container displays a failed state: the execution stage shows "Research failed" with a red status badge (deep crimson #DC2626). Inside the clean progress box, a calm, neutral, user-safe error summary banner (#FDF3F1 subtle warm red surface, crimson border #DC2626) displays the exact copy: "Research couldn't be completed. Please try again." Directly beside the error message is a clear "Retry" action button (#D95338 terracotta coral outline, 6px radius). The UI does NOT mention specific backend providers (Exa, Firecrawl, OpenRouter, Writer), does NOT leak raw Python stack traces, API keys, HTTP 500 codes, or internal JSON payloads. The bottom composer is re-enabled for new inputs. The layout preserves open warm cream canvas dominance (#FBF9F5) and deep charcoal typography (#1F1E1C).

SCREEN 10: High-fidelity mobile web UI for Desearch AI (390x844 viewport, iPhone 14 standard) displaying a completed research workspace adapted for narrow mobile screens, maintaining full visual brand continuity with SCREEN 01–09. The desktop 260px sidebar is hidden off-canvas. The top mobile header (#FFFFFF surface, bottom warm border #E6E2D8, 56px height) displays a left hamburger menu icon trigger (3-lines), the "Desearch AI" logo mark in clean sans-serif typography, and a right "Reports (8)" badge button. The main viewport is a single-column flex container on a warm cream canvas background (#FBF9F5) displaying: the user query heading ("Compare FastAPI and Flask..."), a compact summary bar ("✓ Research completed · 8 sources"), the synthesized report answer formatted in readable editorial serif typography (EB Garamond, 22px title, 15px body, 1.5 line height), inline citations [1] [2], a compact vertical Sources list, a Report Artifact card ("Open Report"), and a sticky mobile composer at the bottom with placeholder text "Start another research question" (no conversational follow-up memory implied). Touch targets are large and accessible (minimum 44x44px). There is NO squeezed 3-column desktop layout on mobile.

SCREEN 11: High-fidelity mobile web UI for Desearch AI (390x844 viewport, iPhone 14 standard) displaying the full-screen Mobile Reports Sheet overlay, launched by tapping the "Reports (8)" top badge in SCREEN 10. The mobile viewport is completely covered by the full-screen Reports sheet overlay (#FFFFFF clean surface tone, 100% viewport width and height). The sheet header features a clear title "Reports & Evidence" and a prominent right "[Done]" / "[Close]" button (no drag handles or complex drag-to-dismiss gestures required). The sheet content includes: a top Canonical Report Summary card ("FastAPI vs Flask Synthesis Report · 8 Sources · 520 Words") with an "Open Full Report" button, prominent touch-friendly export action buttons "[⬇ Download PDF (.pdf)]" and "[⬇ Download Markdown (.md)]" styled in terracotta coral outline (#D95338, 48px height), and a full scrollable list of all 8 research source items displaying favicons, article titles, domain URLs, and short excerpts. Terminology strictly uses "Research Sources" (NOT "Verified Sources"). Typography, colors (#FBF9F5 canvas, #1F1E1C text, #D95338 accent), and radii strictly match SCREEN 01–10.
