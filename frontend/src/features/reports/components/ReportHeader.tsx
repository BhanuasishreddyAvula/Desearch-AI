import React from 'react';

interface ReportHeaderProps {
  title?: string;
}

export const ReportHeader: React.FC<ReportHeaderProps> = () => {
  // Start directly with the report content without repeating user query title
  return null;
};
